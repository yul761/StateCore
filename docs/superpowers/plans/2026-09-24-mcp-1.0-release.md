# statecore-mcp 1.0.0 release (sub-project 5) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Ship `statecore-mcp@1.0.0` with the frozen tool surface documented, the Claude Code plugin pinned to it, a git tag and GitHub release, the npm publish, the Glama release, and the downstream `dsh-statecore` bump.

**Architecture:** No product code changes. A major changeset drives `changeset version` to 1.0.0; the plugin files follow the package version (test-enforced); the release commit lands on `main` through a PR so CI runs the MCP suite on Node 22; publishing steps that need credentials (npm, Glama) are executed by whoever holds them.

**Spec:** `docs/superpowers/specs/2026-09-23-statecore-mcp-1.0-design.md` (section "Sub-project 5 — release").

## Global Constraints

- The six tools (`remember`, `recall`, `facts`, `why`, `forget`, `handoff`) keep their names and input schemas from 0.6.0; 1.x changes are additive only. Public CLI surface: the MCP server (default), `export`, `hook <event>`, `digest`. Data-file promise: any 1.x opens any earlier 1.x/0.6.x file.
- Explicitly out of 1.x: semantic retrieval in embedded mode; a resident daemon.
- Tag name `statecore-mcp@1.0.0` (repo `v1.x` tags belong to apps/api). Glama Auto-Release is off; the Glama release is manual.
- `plugins/claude-code/.claude-plugin/plugin.json` `version`, `hooks/hooks.json` pins, `.mcp.json` pin and `.claude-plugin/marketplace.json` `metadata.version` all equal `apps/mcp/package.json` `version` (`tests/plugin-config.test.ts` enforces).
- `npm publish` and the Glama release need the user's credentials (`npm whoami` currently returns E401); stop and hand over at that step rather than guessing.
- Commit messages end with the session's attribution lines.

---

### Task 1: Stability section, major changeset, version bump, plugin pins

**Files:** `apps/mcp/README.md`, `.changeset/mcp-1-0.md` (new, consumed by `changeset version`), `apps/mcp/package.json` + `apps/mcp/CHANGELOG.md` + root `CHANGELOG.md` (generated), `plugins/claude-code/.claude-plugin/plugin.json`, `plugins/claude-code/hooks/hooks.json`, `plugins/claude-code/.mcp.json`, `.claude-plugin/marketplace.json`.

- [ ] **Step 1: README Stability section** — add before "## Data file compatibility" in `apps/mcp/README.md`:

```markdown
## Stability

`statecore-mcp` 1.x makes four promises:

1. **Tools.** `remember`, `recall`, `facts`, `why`, `forget` and `handoff` keep their names and input schemas. New optional inputs and new output fields may appear; nothing is renamed or removed within 1.x.
2. **CLI.** The default command runs the MCP server over stdio; `statecore-mcp export`, `statecore-mcp hook <event>` and `statecore-mcp digest` are public subcommands with the flags documented here.
3. **Data files.** Any 1.x release opens a database written by any earlier 1.x or 0.6.x release and upgrades it in place (see "Data file compatibility").
4. **Install.** No install-time scripts and no native modules; Node 22.13 or newer is the only requirement.

Not in 1.x, on purpose: semantic (embedding) retrieval in embedded mode, and a resident single-machine daemon. Both are tracked for a later major.
```

- [ ] **Step 2: Major changeset** — `.changeset/mcp-1-0.md`:

```markdown
---
"statecore-mcp": major
---

statecore-mcp 1.0.0.

The tool surface (`remember`, `recall`, `facts`, `why`, `forget`, `handoff`) and the CLI (`export`, `hook`, `digest`) are now stable and additive-only; data files are versioned and upgrade in place; installation runs no scripts and needs only Node ≥ 22.13. See the README's "Stability" section for the exact promises and the two things deliberately left out of 1.x (embedded semantic retrieval, a resident daemon).

Breaking against 0.6.x, all documented in the earlier 1.0 pre-release changesets: Node floor 22.13; `runScopeDigest({ prisma })` → `runScopeDigest({ db })`; the `facts` MCP tool returns `{ groups, pending? }` instead of a bare array.
```

- [ ] **Step 3: Version** — `GITHUB_TOKEN="$(gh auth token)" pnpm exec changeset version`. Confirm `apps/mcp/package.json` is `1.0.0`, `apps/mcp/CHANGELOG.md` has a `## 1.0.0` section aggregating the four changesets (node:sqlite, hooks, keyless visibility, 1.0), and the api/contracts/core packages got minor bumps from the factId changeset. Delete no generated content.

- [ ] **Step 4: Plugin pins** — set `plugin.json` `version` to `1.0.0`; replace every `statecore-mcp@0.6.0` in `hooks/hooks.json` and `.mcp.json` with `statecore-mcp@1.0.0`; `marketplace.json` `metadata.version` `1.0.0`. Update `plugins/claude-code/README.md`'s "coming in 1.0" phrasing to present tense.

- [ ] **Step 5: Verify** — `pnpm --filter statecore-mcp test` (plugin-config test now enforces 1.0.0 everywhere), `pnpm lint`, and `pnpm --filter statecore-mcp exec pnpm pack --pack-destination /tmp/sc-pack-1.0` then in a scratch dir `npm install --ignore-scripts /tmp/sc-pack-1.0/statecore-mcp-1.0.0.tgz` and a stdio `initialize` probe reporting `"version":"1.0.0"`.

- [ ] **Step 6: Commit** — `release: statecore-mcp 1.0.0 — stable tools, versioned data files, Claude Code hooks`.

### Task 2: land on main, tag, GitHub release

- [ ] Push the branch, open the PR (`statecore-mcp 1.0.0 release`), wait for CI green (`gh pr checks --watch`), merge with a merge commit.
- [ ] On the updated `main`: `git tag -a statecore-mcp@1.0.0 <merge-sha> -m "statecore-mcp 1.0.0"`, `git push origin statecore-mcp@1.0.0`, `gh release create statecore-mcp@1.0.0 --title "statecore-mcp 1.0.0" --notes-file <the 1.0.0 section of apps/mcp/CHANGELOG.md>`.

### Task 3: publish (credentials required)

- [ ] `npm whoami` — if it fails, ask the user to run `npm login` (or `! npm login` in this session) and stop until done.
- [ ] `cd apps/mcp && pnpm publish --access public --no-git-checks` (from the merged `main` checkout, after `pnpm --filter statecore-mcp bundle`); verify `npm view statecore-mcp version` prints `1.0.0`.
- [ ] Glama: browser → `glama.ai/mcp/servers/yul761/StateCore/admin/dockerfile` → Build (spec is unchanged; the clone picks up the new head) → when the test passes, Create Release `1.0.0` with the changelog summary.
- [ ] Smoke the plugin path: `npx -y statecore-mcp@1.0.0 hook session-start --data /tmp/sc-smoke` with a minimal payload on stdin exits 0.

### Task 4: dsh-statecore 0.4.0

- [ ] Clone `yul761/dsh-statecore` to the scratchpad; bump `statecore-mcp` to `^1.0.0` in `package.json`; `pnpm install`; `pnpm build && pnpm test`; note that `createEmbeddedBackend`, `listScopes`, `digestNow` are unchanged and `remember`'s return is additive. Add a changeset/bump to `0.4.0`, commit `chore: statecore-mcp ^1.0.0`, push, CI green.
- [ ] `npm publish` (user credentials) and a GitHub release `v0.4.0` on that repo.

### Task 5: announce and record

- [ ] Reply on deepseek-harness discussion #2146 with a short 0.4.0/1.0 update; update the memory files (`mcp-1.0-progress`, `glama-listing-pending`).
